Backend:
DONE
- make File object
- get an image in the object
- random image get function
- make cache
- refresh cache every x minutes
- cache x images
TODO


Frontend:
DONE
- remake frontend
- get input working
- get previous working
- call back end random image
TODO
- find how much data each image is using
- limit storing up to the last 20 images?


LONG TERM:
- docker
- metrics (grafana)
- tracing
- db
-- view history
-- store images in db?
- load balancer
- openapi
DONE
- cache
-- have 20% of total images ready to serve


NOTES:
- a lot of the image sets are numbered. If I'm adding a db, maybe I can set it so that there's a column for the set they belong to. The frontend will recieve a detail about that set so the user knows that there are related images. They can click to show the next and previous images of that set, which should show the rotations
- user table, track stats
- maybe have the next image loaded by the 30 second mark, or even send them in batches? that gets complicated though if we wanna try the set idea
- how much can i compress an image before it's noticable



Frontend
    Display image with graceful loading (spinner / fade-in)
Image Source
    Serve public URLs
Security / Robustness
    Rate limiting per IP
    Input validation (if any)
    CORS configuration
    HTTPS via hosting provider
    CSP headers (Content Security Policy)
Testing
    Unit tests for backend logic
    Smoke test: is /random-image reachable?
    Lighthouse / performance test on frontend
Bonus Ideas
    Admin dashboard: Upload/remove images
    User accounts: Save favorite images
    Tagging: Serve random image by tag (e.g., GET /random-image?tag=nature)
    Real-time mode: Use WebSockets to push a new image every minute
    CI/CD: Set up GitHub Actions for deployment pipeline
    Dockerization: Containerize backend